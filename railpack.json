{
  "build": {
    "cmd": "npm run build --workspace @gighub/shared && npm run build --workspace @gighub/api"
  },
  "start": {
    "cmd": "npm run db:migrate:deploy --workspace @gighub/api && npm run start --workspace @gighub/api"
  }
}
